{
  "name": "note-lock",
  "version": "1.0.5",
  "description": "Lock notes against editing until unlocked, with a frontmatter-synced lock that blocks the editor and other plugins.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit -p tsconfig.json && node esbuild.config.mjs production",
    "publish:vault": "npm run build && node publish-vault.mjs",
    "ship": "npm run publish:vault && git push origin",
    "lint": "eslint src/**/*.ts",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "lock",
    "read-only",
    "protect",
    "freeze"
  ],
  "author": "Robert Fleming",
  "license": "MIT",
  "devDependencies": {
    "@codemirror/state": "^6.7.2",
    "@codemirror/view": "^6.38.6",
    "@types/node": "^24.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.39.4",
    "eslint-plugin-obsidianmd": "^0.4.0",
    "globals": "^17.6.0",
    "obsidian": "latest",
    "typescript": "^5.5.0"
  }
}
